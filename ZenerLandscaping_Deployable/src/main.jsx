import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import ZenerLandingPage from "./ZenerLandingPage";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ZenerLandingPage />
  </React.StrictMode>
);